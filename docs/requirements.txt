# Documentation dependencies
# These are optional - only needed if you want to generate documentation locally

# Quarto is installed separately (not via pip)
# See: https://quarto.org/docs/get-started/

# Python dependencies for documentation generation script
# (Already in main project requirements, listed here for reference)
# No additional dependencies needed - uses standard library

